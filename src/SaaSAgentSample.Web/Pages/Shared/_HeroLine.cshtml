@{
    // Element 1: the one-line framing that sits above every page. Rendered from _Layout, i.e.
    // outside <main>, so wwwroot/js/site.js must also swap .hero-line on a language change.
}
<div class="hero-line">
    <p>@L["Marketplace handles the money. You build the part that turns the service on and keeps its state — via Resolve/Activate at start and the Webhook during operation."]</p>
</div>
