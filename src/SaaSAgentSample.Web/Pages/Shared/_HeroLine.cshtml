@{
    // Optional framing copy for an expanded teaching view, not shared product chrome.
}
<div class="hero-line">
    <p>@L["Marketplace handles billing. The partner company activates the service and keeps contract state through Resolve/Activate and webhooks."]</p>
</div>
